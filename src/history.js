//custom history object, to have easier access to history
//createBrowserHistory - dependency of create router dom
import { createBrowserHistory } from 'history';

export default createBrowserHistory();
